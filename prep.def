BOARD_WIDTH := 14
BOARD_HEIGHT := 16

CURSOR_LEFT := "("
CURSOR_RIGHT := ")"

FLAG_SYMBOL := "⚐"
UNKNOWN_SYMBOL := "."
-- Because Ada is a dumb language you cannot for some
-- reason have "*" in a def file so to combat this you
-- must add a zero-width character before the * (​)
-- "​*"
MINE_SYMBOL := "​*"
ZERO_COUNT_SYMBOL := " "
